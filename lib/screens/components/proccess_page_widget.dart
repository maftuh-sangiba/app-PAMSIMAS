import 'package:flutter/material.dart';

class ProccessPageWidget extends StatelessWidget {
  const ProccessPageWidget({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: CircularProgressIndicator(),
    );
  }
}
